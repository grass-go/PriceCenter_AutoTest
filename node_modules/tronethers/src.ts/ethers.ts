"use strict";

import { BaseContract, Contract, ContractFactory } from "@tronproject/contracts";

import { BigNumber, FixedNumber } from "@tronproject/bignumber";

import { Signer, VoidSigner } from "@tronproject/abstract-signer";
import { Wallet } from "@tronproject/wallet";

import * as constants from "@tronproject/constants";

import * as providers from "@tronproject/providers";
import { getDefaultProvider } from "@tronproject/providers";

import { Wordlist, wordlists} from "@tronproject/wordlists";

import * as utils from "./utils";

import { ErrorCode as errors, Logger } from "@tronproject/logger";

////////////////////////
// Types

import { BigNumberish } from "@tronproject/bignumber";
import { Bytes, BytesLike, Signature } from "@tronproject/bytes";
import { Transaction, UnsignedTransaction } from "@tronproject/transactions";


////////////////////////
// Compile-Time Constants

// This is generated by "npm run dist"
import { version } from "./_version";

const logger = new Logger(version);

////////////////////////
// Types

import {
    ContractFunction,
    ContractReceipt,
    ContractTransaction,

    Event,
    EventFilter,

    Overrides,
    PayableOverrides,
    CallOverrides,

    PopulatedTransaction,

    ContractInterface
} from "@tronproject/contracts";


////////////////////////
// Exports

export {
    Signer,

    Wallet,
    VoidSigner,

    getDefaultProvider,
    providers,

    BaseContract,
    Contract,
    ContractFactory,

    BigNumber,
    FixedNumber,

    constants,
    errors,

    logger,

    utils,

    wordlists,


    ////////////////////////
    // Compile-Time Constants

    version,


    ////////////////////////
    // Types

    ContractFunction,
    ContractReceipt,
    ContractTransaction,
    Event,
    EventFilter,

    Overrides,
    PayableOverrides,
    CallOverrides,

    PopulatedTransaction,

    ContractInterface,

    BigNumberish,

    Bytes,
    BytesLike,

    Signature,

    Transaction,
    UnsignedTransaction,

    Wordlist
};

